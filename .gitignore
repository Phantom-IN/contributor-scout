# Contributor Scout discovery output (generated per analysed repository)
contribution-discovery/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local host overrides. The hook config files in particular are auto-loaded by
# their host from these paths, so a local copy made for testing must never be
# committed - see hooks/README.md.
.claude/settings.local.json
.cursor/hooks.json
.agents/hooks.json

# Maintainer-local launch/marketing material - not part of the public project
marketing/
