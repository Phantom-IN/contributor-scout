{
  "_comment": [
    "EXAMPLE ONLY - this file is not read from here. Copy it to",
    "<target-repo>/.cursor/hooks.json (the repository you are analysing, not",
    "this one) and replace /ABSOLUTE/PATH/TO/contributor-scout with the real",
    "path to this checkout. Read hooks/README.md first - it blocks git commit,",
    "git push, gh write commands, and writes outside contribution-discovery/,",
    "which will interfere with ordinary development work in the same session.",
    "Do NOT place this file at .cursor/hooks.json inside the contributor-scout",
    "checkout itself - Cursor auto-loads project hooks from there, and the",
    "placeholder script path below does not exist yet.",
    "Both events are registered deliberately: preToolUse covers every tool",
    "including file writes, and beforeShellExecution is the dedicated terminal",
    "gate that also runs for cloud agents. Shell commands are therefore checked",
    "twice, which is harmless - the guard is idempotent."
  ],
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "type": "command",
        "command": "python3 /ABSOLUTE/PATH/TO/contributor-scout/hooks/discovery_guard.py",
        "timeout": 10
      }
    ],
    "beforeShellExecution": [
      {
        "type": "command",
        "command": "python3 /ABSOLUTE/PATH/TO/contributor-scout/hooks/discovery_guard.py",
        "timeout": 10
      }
    ]
  }
}
