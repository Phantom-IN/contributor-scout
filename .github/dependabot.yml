# The project has zero runtime dependencies by design; the only third-party
# code it consumes is the pinned GitHub Actions in ci.yml. Keep those patched.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    labels:
      - dependencies
